# Lead Genius Admin - Variáveis de Ambiente
# Copie este arquivo para .env e preencha com suas chaves do Supabase

# ===========================================
# SUPABASE CONFIGURATION
# ===========================================

# URL do seu projeto Supabase
# Encontre em: Settings > API > Project URL
SUPABASE_URL=https://seu-projeto.supabase.co

# Chave anônima (pública) do Supabase
# Encontre em: Settings > API > Project API keys > anon public
SUPABASE_ANON_KEY=sua-chave-anonima-aqui

# Chave de serviço (apenas para scripts de seed/admin)
# NUNCA exponha esta chave no cliente!
# Encontre em: Settings > API > Project API keys > service_role
SUPABASE_SERVICE_ROLE_KEY=sua-chave-de-servico-aqui

# ===========================================
# APP CONFIGURATION
# ===========================================

# Ambiente atual (development, staging, production)
APP_ENV=development

# URL base da aplicação (para deep linking)
APP_BASE_URL=http://localhost:3000

# ===========================================
# OPTIONAL FEATURES
# ===========================================

# Habilitar logs de debug
ENABLE_DEBUG_LOGS=true

# Timeout para requisições HTTP (em segundos)
HTTP_TIMEOUT=30
